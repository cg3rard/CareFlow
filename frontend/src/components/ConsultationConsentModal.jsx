import { useEffect, useState } from 'react';
import { useFlow } from '../context/FlowContext';

export default function ConsultationConsentModal({ onAccept, onDecline, onClose }) {
  const { psychologists, loadPsychologists, selectPsychologist, authError } = useFlow();
  const [stage, setStage] = useState('consent'); const [selectingId, setSelectingId] = useState(null);
  useEffect(() => { void loadPsychologists(); }, []);
  const shareAndChoose = () => { onAccept(); setStage('choose'); };
  const choose = async (psychologist) => { setSelectingId(psychologist.id); try { await selectPsychologist(psychologist.id, true); onClose(); } finally { setSelectingId(null); } };
  return <div className="fixed inset-0 z-[70] flex items-center justify-center bg-black/45 p-4 backdrop-blur-sm" role="dialog" aria-modal="true" aria-labelledby="consultation-consent-title"><div className="w-full max-w-md rounded-[2rem] border-2 border-[#121214] bg-surface-container-lowest p-6 shadow-[0_8px_0_#121214] sm:p-8"><div className="flex items-start justify-between gap-4"><div className="flex h-12 w-12 items-center justify-center rounded-2xl bg-tertiary-container text-on-tertiary-container"><span className="material-symbols-outlined text-[26px]">psychology</span></div><button onClick={onClose} className="flex h-9 w-9 items-center justify-center rounded-full bg-surface-container text-on-surface" aria-label="Close">×</button></div>{stage === 'consent' ? <><h2 id="consultation-consent-title" className="mt-5 text-2xl font-bold tracking-tight text-on-surface">Share your data?</h2><p className="mt-2 text-sm font-medium leading-relaxed text-on-surface-variant">Do you want to share your data with our psychologist to support your mental health?</p><p className="mt-3 rounded-2xl bg-surface-container p-3 text-xs leading-relaxed text-on-surface-variant">If you agree, the psychologist you choose can view your session summaries and the Brain Dump notes you've approved. You can still chat without sharing your data.</p><div className="mt-6 grid gap-2 sm:grid-cols-2"><button onClick={onDecline} className="rounded-full bg-surface-container px-4 py-3 text-xs font-bold text-on-surface">No, chat only</button><button onClick={shareAndChoose} className="rounded-full bg-tertiary px-4 py-3 text-xs font-bold text-on-tertiary shadow-[0_3px_0_#121214]">Yes, share my data</button></div></> : <><span className="mt-5 block text-xs font-bold uppercase tracking-widest text-tertiary">Step 2 · choose a psychologist</span><h2 className="mt-1 text-2xl font-bold tracking-tight text-on-surface">Who's your consultant?</h2><p className="mt-2 text-sm text-on-surface-variant">Choose one psychologist. Your consultant will stay locked in until you disconnect.</p>{authError && <p className="mt-3 rounded-xl bg-error-container p-3 text-xs font-bold text-on-error-container">{authError}</p>}<div className="mt-5 space-y-3">{psychologists.length === 0 ? <p className="rounded-xl bg-surface-container p-4 text-sm text-on-surface-variant">Loading psychologists…</p> : psychologists.map((psychologist) => <button disabled={selectingId !== null} onClick={() => choose(psychologist)} key={psychologist.id} className="flex w-full items-center justify-between rounded-2xl bg-surface-container p-4 text-left transition-colors hover:bg-tertiary-container disabled:opacity-60"><span><strong className="block text-sm text-on-surface">{psychologist.name}</strong><span className="mt-1 block text-xs text-on-surface-variant">Careflow Psychologist · choose to share data</span></span><span className="material-symbols-outlined rounded-full bg-surface-container-lowest p-2 text-tertiary">{selectingId === psychologist.id ? 'progress_activity' : 'arrow_forward'}</span></button>)}</div><button onClick={() => setStage('consent')} className="mt-5 text-xs font-bold text-tertiary">← Back to consent options</button></>}</div></div>;
}
